# CLD2 roadmap after alpha56

## Recommended next steps

1. **alpha56.1 documentation polish**
   - ensure README links point to the new docs;
   - keep quickstart commands correct;
   - remove or quarantine any broken legacy scripts.

2. **alpha57 optional MinIO/S3 real object-store probe**
   - use local MinIO only if the environment is stable;
   - measure correctness and provider behavior first, not performance claims.

3. **alpha58 optional cloud/CDN design note**
   - architecture only unless real infrastructure is available;
   - no production-performance claim without real measurements.

4. **GitHub publication checklist**
   - use MIT license;
   - publish source without paywall;
   - donation/support link optional;
   - keep claim boundary visible.
