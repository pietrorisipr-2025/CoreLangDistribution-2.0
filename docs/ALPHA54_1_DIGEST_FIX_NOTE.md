# Alpha54.1 fixed digest note

The alpha54 object-store-like demo initially compared installed payloads including the `.cld2` install metadata directory. Alpha54.1 fixes the comparison by excluding `.cld2` when computing the installed payload tree digest.

The validated alpha54.1 review result is included in the alpha56 release assets.
