# Dist-check note

The embedded `dist-check` command still contains a historical warning that README.md does not mention `alpha24`. In alpha50.2 this is intentionally not treated as a failure: the public README is updated to alpha50.2.
