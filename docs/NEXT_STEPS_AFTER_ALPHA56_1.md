# Next steps after alpha56.1

Recommended order:

1. Publish the alpha56.1 repo candidate privately on GitHub first.
2. Review README rendering and links.
3. Attach release assets only if desired; do not upload old heavy recovery packs.
4. Optional alpha57: MinIO/S3 real object-store demo, only if we want a genuine object-store run with clear claim boundary.
5. Optional alpha58: CI workflow polish and GitHub Actions badge.

Avoid new heavy benchmarks before the public repository is readable and stable.
